version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"  # Change from 3000 to 80 for production
    # REMOVE all volumes - not needed for production
    # volumes:
    #   - ./:/app
    #   - /app/node_modules
    #   - /app/build
    environment:
      - NODE_ENV=production  # Change from development
      - PORT=80
    # REMOVE development settings:
    # stdin_open: true
    # tty: true
    # command: npm start